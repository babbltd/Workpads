# Templates

This folder contains starter templates for Workpads PADS records.

Formats:

- `*.kv` author-friendly simple format
- `*.yaml` author-friendly structured format
- `runtime/*.json` normalized runtime schema used by services

The current starter template is `svc-basic` for single-document job records.
